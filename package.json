{
  "name": "automation",
  "version": "1.0.0",
  "description": "Rework project ",
  "main": "conf.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "github.com/RapeikaD/automation"
  },
  "keywords": [
    "protractor",
    "automation"
  ],
  "author": "Rapeika D",
  "license": "ISC",  
  "dependencies": {},// look here! and 1 string below
  "devDependencies": {
    "allure-commandline": "^2.7.0",
    "jasmine-allure-reporter": "^1.0.2"
  },
  "scripts": {
        "pretest": "rm -rf allure-report",
        "test": "protractor conf.js",
        "posttest": "allure generate allure-results --clean -o allure-report || true"
    }

}
